matrix:
    include:
      - language: python
        python:
          -"3.6"
        install:
          - pip install -r requirements.txt
        script:
         - pytest -v
       
     - language: node_js
       node_js: "8"
       script:
         - npm test
         - npm run build
         
       
